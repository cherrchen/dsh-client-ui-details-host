/**
 * Details Host plugin, node half. Empty apply exists so the plugin appears
 * in Host cordis.yml / Loader; the browser half ships via exports["./client"].
 */

/** Host plugin body — no host-side behavior. */
export function apply(): void {}
